{
  "name": "simple-backend-ci-cd",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal Express backend with a GitHub Actions CI/CD pipeline.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
